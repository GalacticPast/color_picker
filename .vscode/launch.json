{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Existing Binary",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/app"
        }
    ]
}